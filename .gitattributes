# Binary assets are stored directly in git (no LFS) so GitHub Pages can serve
# them as actual binaries rather than LFS pointer text. Each file in this
# repo stays well under GitHub's 100 MB hard limit.
*.mp4 binary
*.png binary
*.jpg binary
